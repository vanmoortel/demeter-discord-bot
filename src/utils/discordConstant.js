/**
 * Slash Command
 * @type {{ROLE: number, SUB_COMMAND_GROUP: number, SUB_COMMAND: number, NUMBER: number, CHANNEL: number, MENTIONABLE: number, STRING: number, USER: number, BOOLEAN: number, INTEGER: number}}
 */
export const ApplicationCommandOptionTypes = {
    SUB_COMMAND: 1,
    SUB_COMMAND_GROUP: 2,
    STRING: 3,
    INTEGER: 4,
    BOOLEAN: 5,
    USER: 6,
    CHANNEL: 7,
    ROLE: 8,
    MENTIONABLE: 9,
    NUMBER: 10,
}